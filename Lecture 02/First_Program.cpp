// We will write our first program to print some text on the terminal.
#include <iostream>  //Header File Inclusion
using namespace std; // C++ Standard Library (that includes keywords like cout, cin, etc) is in a namespace called std
int main()
{                                     // Main Function starts
    cout << "Hello Universe" << endl; // cout<< "it prints anything written inside it on the terminal "<< endl is used to move the cursor to the next line in terminal;
    return 0;                         // returns 0 as an integer to the main function
}